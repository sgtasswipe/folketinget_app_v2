# Clone Project

# Install packages

npm install

# Run using Expo

npx expo start

or alternatively use npx expo start --tunnel
if not on the same network as mobile device.
Can also be tested in browser by hitting "w" after startup.
